const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "db4free.net",
      user: "daikin_admin",
      password: "Comsci10@",
      database: "daikin_db1",
    },
  };
  module.exports = config;